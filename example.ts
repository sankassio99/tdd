


describe("Deve criar um todo item", () => {
    // Arrange
    // initialization varaibles necessary for the test
    const exampleVariable = "example";
    
    // Act
    // call the function or method to be tested
    const result = exampleFunction(exampleVariable);

    // Assert
    // check if the result is as expected
    const expectedValue = "expectedValue";
    expect(result).toBe(expectedValue);
})

describe("When [something happen] should [receive that]", () => {
    // Arrange
    // initialization varaibles necessary for the test
    const requestInput = "example";
    
    // Act
    // call the function or method to be tested
    const response = doSomething(requestInput);

    // Assert
    // check if the response is as expected
    const expectedValue = "expectedValue";
    expect(response).toBe(expectedValue);
})